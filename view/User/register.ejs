<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-black text-white">
    
    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="w-100" style="max-width: 400px;">
            <h1 class="text-center mb-4">Registration Form</h1>

            <form action="" method="post" class="bg-black p-4 rounded">
                <div class="mb-3">
                    <label for="name" class="form-label">Name</label>
                    <input type="text" name="name" id="name" class="form-control" placeholder="Enter Name" pattern="^[^\s]*$" required>
                </div>
                <div class="mb-3">
                    <label for="email" class="form-label">Email address</label>
                    <input type="email" name="email" id="email" class="form-control" placeholder="Enter Email" pattern="^[^\s]*$" required>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" name="password" id="password" class="form-control" placeholder="Enter Password" pattern="^[^\s]*$" required>
                </div>
                <div class="mb-3">
                    <label for="mobile" class="form-label">Phone</label>
                    <input type="mobile" name="phone" id="phone" class="form-control" placeholder="Enter Mobile Number" pattern="^[^\s]*$" required>
                </div>
                <button type="submit" class="btn w-100" style="background-color: #b0b0b0; color: white;">Register</button>
            </form>

            <% if (typeof message !== 'undefined') { %>
                <p class="text-success text-center mt-3"><%= message %></p>
            <% } %>

            <p class="text-center mt-3">Have an account? <a href="/" class="text-white">Login</a></p>
        </div>
    </div>

    <!-- Bootstrap JS (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
